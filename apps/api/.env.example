DATABASE_URL="postgresql://tms:tms@localhost:5455/tms_g3?schema=public"
JWT_SECRET="dev-only-change-me"
JWT_EXPIRES_IN="8h"
PORT=3011

# AI image extraction is disabled until data-sharing policy and provider are approved.
AI_EXTERNAL_PROCESSING_ENABLED=false
AI_EXTRACTION_MODE=vlm_then_ocr
AI_IMAGE_HOST_ALLOWLIST=storage.example.com
AI_REQUEST_TIMEOUT_MS=30000
# OpenAI-compatible VLM gateway (for example a provider or an internal gateway).
AI_VLM_ENDPOINT=
AI_VLM_API_KEY=
AI_VLM_MODEL=
# Optional OCR HTTP gateway, expected response: { "text": "...", "confidence": 0.0 }
AI_OCR_ENDPOINT=
AI_OCR_API_KEY=
